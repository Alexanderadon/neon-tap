/** Crystal economy: the daily allowance, the login calendar, first clears, the second chance (package A). Keys must not repeat those of other parts (see ru.test.ts). */
export const economy = {} as const;
