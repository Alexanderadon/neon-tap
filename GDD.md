# NEON TAP — GDD

> Игровой дизайн-документ и технический план. Читается Claude Code перед каждой фазой (см. `CLAUDE.md`).
> Версия 1.1 — адаптирована под React + TypeScript + Feature-Sliced Design.

---

## 0. Музыка: почему в игре нет чужих хитов

Нарушение авторского права возникает в момент **воспроизведения и распространения**, а не получения прибыли. Некоммерческое использование влияет на размер компенсации, а не на законность. Практические последствия для хостинга:

| Где хостится | Что произойдёт с лицензионными треками |
|---|---|
| GitHub Pages / Vercel | DMCA takedown → проект отключают, аккаунт получает страйк |
| Резюме | **мёртвая ссылка** — худший исход для портфолио |

Piano Tiles-клоны с лицензионными треками снимают регулярно; osu! держит отдельный DMCA-процесс; Beat Saber и Guitar Hero покупали лицензии. Тимлид, глядящий портфолио, знает про DMCA — игра с ворованными треками читается как минус.

### Двухслойная модель контента

1. **Встроенный саундтрек** — 16 треков под **CC0** (public domain) с OpenGameArt: synthwave, EDM, drum&bass, hardstyle, techno. Реестр: `assets-src/tracks.json`, отчёт по лицензиям: `public/music/LICENSES.md`.
2. **«Своя музыка»** — игрок кидает MP3/OGG/WAV, движок сам генерирует карту через onset-detection в Web Audio API. Файл не покидает браузер, ничего не распространяется → нарушения нет.

Второй пункт важнее первого: автогенерация карт из произвольного аудио — единственная фича, которую нельзя списать с туториала. Именно она в первом абзаце README.

### Почему OpenGameArt, а не Pixabay

Pixabay требует логин для скачивания и не отдаёт музыку через API — пайплайн нельзя воспроизвести скриптом. OpenGameArt даёт прямые ссылки и CC0-фильтр; каждый файл в репозитории восстанавливается командой `npm run assets:music` из реестра.

---

## 1. Что даёт зависимость — исследование → механики

| Рычаг | Механика | Где в коде |
|---|---|---|
| **Комбо и страх его потерять** (loss aversion) | Комбо крупно, шрифт `32 + min(combo, 500)·0.06`; цвета на порогах 50 → cyan, 100 → фиолетовый, 250 → золото, 500 → белый с пульсацией. При промахе комбо разлетается частицами + звук «стекло». | `features/play-chart/lib/Renderer.ts`, `shared/lib/audio/sfx.ts` |
| **Промах ломает музыку** | Lowpass 800 Гц на мастер-канал + −40 % громкости на 250 мс. Наказание слышно, а не только видно. | `AudioEngine.missEffect()` |
| **Near-miss** | Экран результатов: разбивка Perfect/Great/Good/Miss и «до ранга S не хватило N нот». RETRY — крупнейшая кнопка, в фокусе. | `entities/score/notesToReach`, `widgets/result-breakdown` |
| **Нулевое трение рестарта** | Трек в памяти (`AudioBuffer`); рестарт = сброс индексов + `source.start()`. Хоткей `R`. Переходы ≤ 200 мс. | `GameSession.restart()` |
| **Flow** | Звёзды 1–10 на карту (по плотности нот), три сложности, следующий мир — по накопленным звёздам. | `ChartGenerator.rateStars`, `entities/world` |
| **Незакрытый прогресс** (Зейгарник) | Липкий прогресс-бар «★ 12 / 20 до следующего мира» — последнее, что видит игрок перед выходом. | `widgets/world-map` |
| **Juice** | Вспышка полосы 80 мс, пул 300 частиц без аллокаций, shake ≤ 4 px только на промахах и порогах комбо, пульсация фона в такт, предрендеренное свечение (не `shadowBlur` в кадре). | `shared/lib/render/*` |

---

## 2. Референсы

| Игра | Взято | Не взято |
|---|---|---|
| osu!mania | 4 полосы, окна попадания, ранги SS/S/A/B/C/D, точность в % | Перегруженный UI |
| Piano Tiles 2 | Мгновенный рестарт, минимализм | Отсутствие глубины |
| Friday Night Funkin' | Ощущение «клипа», харизматичная подача | Сложность модинга |
| Muse Dash | Яркая стилистика, мобильный онбординг | Гача |
| Beat Saber | Ноты строго по музыкальным акцентам | 3D |
| ADOFAI | Калибровка задержки как обучающий шаг | Однокнопочность |
| Geometry Dash | «Ещё один разок», прогресс-бар уровня | Хардкор без градаций |

---

## 3. Концепт

**Название:** NEON TAP
**Жанр:** 4-полосная ритм-игра, браузер, desktop + mobile
**Сессия:** 90–150 секунд на трек
**Управление:** `D F J K` / стрелки; на мобиле — 4 тач-зоны в нижней половине экрана
**Стиль:** тёмный фон, неоновые полосы (cyan / magenta / lime / orange), CRT-скан-линии и виньетка, глитч-переходы

### Прогрессия

```
МИР 1 «ЗАПУСК»      ★2–6    4 трека   открыт сразу
МИР 2 «ПУЛЬС»       ★2–7    4 трека   нужно 8 ★
МИР 3 «ПЕРЕГРУЗКА»  ★2–9    4 трека   нужно 20 ★
МИР 4 «ЯДРО»        ★2–10   4 трека   нужно 38 ★
СВОЯ МУЗЫКА         авто    ∞         открыта сразу *
```

\* Отклонение от v1.0 (было «нужно 12 ★»): главная фича проекта должна быть доступна рекрутеру без гринда.

Звёзды: 1 за прохождение (ранг ≥ C), +1 за ранг A, +1 за ранг S — лучший результат на любой сложности. 16 треков → максимум 48 ★. Распределение треков по мирам — автоматическое, по звёздам Hard-карты (`scripts/generate-charts.ts`).

### Ранги

| Точность | Ранг |
|---|---|
| 100 % | SS |
| ≥ 95 % | S |
| ≥ 90 % | A |
| ≥ 80 % | B |
| ≥ 70 % | C |
| < 70 % | D (не засчитан) |

### Русская озвучка

16 фраз: «Отлично!», «Молодец!», «Так держать!», «Идеально!», «Не сдавайся!», «Огонь!», «Комбо пятьдесят / сто / двести пятьдесят / пятьсот!», «Полное комбо!», «Поехали!», «Новый рекорд!», «Ранг эс!», «Мимо!», «Ещё разок?».

Реализация: MP3 (Windows SAPI «Microsoft Irina», `scripts/tts.ps1`), фолбэк — Web Speech API `ru-RU`. Отдельный слой громкости. Рейт-лимит 2.5 с, приоритетные фразы (пороги комбо, результат) его обходят.

---

## 4. Технический стек

```
UI         React 18 + TypeScript strict, Feature-Sliced Design
Рендер     Canvas 2D + offscreen-слой статики + предрендер glow-спрайтов
Аудио      Web Audio API — без библиотек
Анализ     собственный FFT → spectral flux → автокорреляция → генератор карт; Web Worker
Сборка     Vite, vitest, eslint (+ boundaries), GitHub Actions
Состояние  собственный store на useSyncExternalStore, localStorage
Хостинг    Vercel
Рантайм-зависимости: react, react-dom. Всё остальное — свой код.
```

**Почему React, если «ноль зависимостей»:** React отвечает только за экраны-меню. Игровой цикл, аудио, судейство, рендер нот и анализ сигнала — чистый TypeScript в `shared/lib` и `features/play-chart`, и именно они — предмет разговора на собеседовании. Открыв `package.json`, работодатель не увидит `phaser` или `tone.js`.

### FSD-раскладка

```
src/
├── app/            App.tsx (роутер экранов), main.tsx, глобальные стили
├── pages/          menu · game · result · calibration · settings · custom
├── widgets/        world-map · game-canvas · result-breakdown · settings-panel · song-drop-zone · calibration-meter
├── features/       play-chart · generate-chart · calibrate-offset · save-result · voice-feedback
├── entities/       track · chart · score · progress · settings · world · play-session
└── shared/
    ├── lib/audio/      AudioEngine · Clock · Conductor · sfx
    ├── lib/analysis/   fft · OnsetDetector · BpmEstimator · ChartGenerator
    ├── lib/render/     neon · Particles · Effects
    ├── lib/input/      Input
    ├── lib/store · lib/router · lib/math
    ├── config/constants.ts   окна, цвета, клавиши, пороги
    ├── i18n/ru.ts
    ├── ui/             Button · Slider · Stars · ProgressBar · Screen
    └── types/
```

Слой импортирует только нижележащие; слайсы одного слоя друг друга не импортируют; чужой слайс — только через `index.ts`. Проверяется `eslint-plugin-boundaries` и `scripts/check-fsd.mjs`.

### Критические технические требования

**1. Единственный источник времени — `audioContext.currentTime`.**

```ts
// shared/lib/audio/Clock.ts
songTime() { return audioNow() - startTime - userOffset; }
```

`requestAnimationFrame` — только для отрисовки. Таймстампы событий ввода (`event.timeStamp`) переводятся в аудиовремя, чтобы компенсировать задержку между нажатием и обработчиком.

**2. Калибровка задержки — при первом запуске.** Метроном 120 BPM планируется look-ahead-планировщиком на аудиочасах; игрок тапает 16 раз; медиана отклонений → `settings.audioOffsetMs`. Ручной слайдер −300…+300 мс в настройках.

**3. Окна попадания.**

```ts
HIT_WINDOWS = { perfect: 0.045, great: 0.090, good: 0.135 } // с; дальше — miss
```

**4. Момент появления ноты.** `approachTime = 1.6 с / scrollSpeed` (1.0–3.0×); `spawn = hitTime − approachTime`, координата `y = hitY − (hitTime − songTime) · hitY / approachTime`.

### Формат карты

```json
{
  "id": "neon-hyperdrive", "title": "Neon Hyperdrive", "artist": "adiutorium",
  "license": "CC0 1.0", "sourceUrl": "https://opengameart.org/content/neon-hyperdrive",
  "audio": "music/neon-hyperdrive.mp3", "bpm": 139.8, "offset": 0.093, "duration": 129.6,
  "charts": {
    "easy":   { "stars": 3, "notes": [[0.500, 0], [1.000, 2]] },
    "normal": { "stars": 6, "notes": [] },
    "hard":   { "stars": 8, "notes": [[12.4, 1, 0.86]] }
  }
}
```

`notes` — `[timeSec, lane]`; длинные ноты — `[timeSec, lane, durationSec]`. Холд даёт два судейства (голова + хвост).

### Автогенератор карт (ключевая фича)

1. `decodeAudioData` → `AudioBuffer` → моно-микс 22 050 Гц (в Node — ffmpeg → f32le).
2. STFT: окно Ханна 1024, hop 512, собственный radix-2 FFT.
3. **Spectral flux**: Σ max(0, log(1+10·|X_t|) − log(1+10·|X_{t−1}|)); отдельно по полосам 20–250 / 250–2000 / >2000 Гц.
4. Адаптивный порог: скользящее среднее ±10 кадров × 1.5 + 2 % от максимума; пик = локальный максимум в ±2 кадра; минимум 50 мс между онсетами.
5. BPM: автокорреляция огибающей flux по лагам 60–220 BPM, бонус за согласие с кратными лагами, параболическая интерполяция, фолд в 90–180, фаза сетки — по максимуму энергии на её узлах.
6. Квантование к 1/8 (Easy) или 1/16 доли; дубли в одном слоте сливаются.
7. Полосы: бас → {0,1}, середина → {1,2}, верх → {2,3}; «≤ 2 подряд в одну полосу», предпочтение чередованию рук; аккорды на сильных ударах (Normal ≥ p93, Hard ≥ p80); холды на сильных ударах перед паузой ≥ 1.5 доли.
8. Плотность: Easy ≤ 3 нот/с, Normal ≤ 6, Hard ≤ 10 (скользящее окно 1 с) + фильтр силы по перцентилю (55 % / 25 % / 0 %) + минимальный интервал 1/2, 1/4, 1/8 доли.
9. Звёзды: `0.6 + avgNps·0.85 + peakNps·0.22 + holdShare`, монотонны по сложностям.

Работает в Web Worker с прогрессом по стадиям. Тот же модуль запускается в Node для встроенных треков — карты игрока и встроенные карты делает один и тот же код.

---

## 5. Тесты

`vitest`, окружение node, без DOM — движок спроектирован так, чтобы тестироваться без браузера:

- `Clock` — дрейф, offset, пауза/резюм
- `Scoring` / `judgeDelta` — окна, точность, ранги, «до S не хватило N»
- `NoteManager` — судейство таймингов, холды, авто-мисс, курсоры
- `OnsetDetector` — синтетический сигнал: ≥ 90 % онсетов в ±30 мс без потока ложных
- `BpmEstimator` — 128 BPM ± 2
- `ChartGenerator` — квантование, кап плотности, «≤ 2 подряд», монотонные звёзды, детерминизм
- `SaveData` — миграции, звёзды, слияние результатов
- `Calibration` — медиана против выброса
- `ParticlePool` / `ScreenShake` — ёмкость, амплитуда ≤ 4 px

---

## 6. Фазы

См. `docs/plans/` — по плану на фазу с чеклистом. Порядок: аудиодвижок → ядро геймплея → визуал → калибровка и мобила → автогенератор → прогрессия и меню → контент → полировка и витрина.

---

## 7. Чеклист портфолио

- [x] Ссылка открывается без регистрации
- [x] Портретная ориентация на телефоне
- [x] README объясняет 4 технических решения
- [x] `package.json` без лишних рантайм-зависимостей
- [x] `LICENSES.md` со всеми источниками музыки
- [x] Тесты в CI (GitHub Actions)
- [ ] GIF геймплея в README — записать с реального прохождения
- [ ] Цифры FPS до/после предрендера glow — снять на throttled CPU
