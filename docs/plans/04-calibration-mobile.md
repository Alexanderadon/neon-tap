# Фаза 4 — Калибровка и мобильная версия

## Задачи
- [x] `features/calibrate-offset/model/Calibration.ts` — 16 тапов, медиана отклонений от ближайшего клика.
- [x] `widgets/calibration-meter` — метроном 120 BPM через look-ahead scheduler на аудиочасах (клики планируются на 200 мс вперёд, `setInterval` только будит планировщик).
- [x] Первый запуск → экран калибровки (`app/App.tsx`), доступна из настроек.
- [x] `widgets/settings-panel` — offset −300…+300 мс, скорость 1.0–3.0×, громкости music/sfx/voice.
- [x] Мобильная раскладка: портрет — полосы во всю ширину, 4 тач-зоны в нижней половине экрана, `touch-action: none`, без выделения/зума/долгого тапа.

## Готово когда
На телефоне игра проходится без ощущения рассинхрона (проверить на реальном Android/iOS — вручную).
