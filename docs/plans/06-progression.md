# Фаза 6 — Прогрессия, меню, сохранения

## Задачи
- [x] `entities/progress` — `localStorage`, версионирование схемы с миграциями, лучший результат по (трек, сложность), звёзды 1/2/3 за C/A/S.
- [x] `entities/world` — 4 мира, пороги 0 / 8 / 20 / 38 ★.
- [x] `widgets/world-map` — карта миров, замки с «нужно N ★», липкий прогресс-бар до следующего мира.
- [x] `widgets/result-breakdown` — ранг крупно, Perfect/Great/Good/Miss, «до ранга S не хватило N нот», RETRY крупнейшая и в фокусе, хоткей R.
- [x] `features/voice-feedback` — 16 русских фраз × 2 нейроголоса (Dmitry / Svetlana, выбор в настройках), фолбэк Web Speech API, рейт-лимит 2.5 с. Первая версия на Windows SAPI «Irina» отбракована как робо-голос.
- [x] Рестарт: `R` / кнопка — сброс индексов + `source.start()`, буфер уже в памяти.

## Отклонение от GDD
«Своя музыка» открыта сразу, а не после 12 ★: это главная фича проекта, и рекрутер должен увидеть её без гринда.

## v1.4 — открытие треков, трек дня, цели (после перехода на плоский список)

Миры и `widgets/world-map` убраны вместе с плоским списком (v1.3); прогрессия живёт в `entities/progress`.

- [x] `entities/progress/model/unlocks.ts` — первые 5 треков открыты, дальше порог `round((позиция − 4) · 2.2)`, кап `3·N − 6`; трек дня и своя музыка открыты всегда. `UNLOCK_ALL = false`, dev-override `?unlock=1` читается только в `shared/config/devFlags.ts`.
- [x] `entities/progress/model/daily.ts` — трек дня: FNV-1a от локальной даты `YYYY-MM-DD` (чистая функция от строки — тесты детерминированы), +1 бонусная ★ раз в день за ранг ≥ C без провала, серия дней.
- [x] `entities/progress/model/goals.ts` — 8 целей из сохранения и счётчиков; награда выдаётся один раз (`goalsClaimed`); награды целей не участвуют в подсчёте цели «20 ★».
- [x] Сохранение v3: `counters` (спеллы по видам, сыгранные треки, макс. комбо, макс. ★ трека), `daily`, `goalsClaimed`; миграция v2 → v3 заполняет счётчики из лучших результатов.
- [x] `features/track-progress` — спелл из события `spell` GameCanvas; `features/save-result` — счётчики, трек дня, выдача целей, `ResultMeta.dailyBonus / goalsCompleted`.
- [x] `widgets/goals-panel` — сворачиваемая панель под шапкой меню, прогресс-бары, галочки, счётчик ★ с бонусами.
- [x] `widgets/track-list` — карточка «ТРЕК ДНЯ» сверху, закрытые карточки затемнены с замком и «нужно ★ N», в шапке треки / бонус.
- [x] `widgets/result-breakdown` — необязательный проп `notes` (строки «Трек дня пройден: +1 ★», «Цель выполнена: …»).
- [x] Тесты: пороги открытия, детерминизм трека дня, оценка целей, миграция v2 → v3, подсчёт бонусных ★, save-result (провал, трек дня, своя музыка).
