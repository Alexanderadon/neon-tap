# Фаза 2 — Ядро геймплея

## Задачи
- [x] `entities/chart` — формат `[time, lane, duration?]`, парсинг/валидация.
- [x] `features/play-chart/model/NoteManager.ts` — пул на 2000 нот, курсоры по полосам, судейство по окнам 45/90/135 мс, холды (голова + хвост), авто-мисс.
- [x] `entities/score` — комбо, множитель, точность (300/200/100/0), ранги SS/S/A/B/C/D, «до ранга S не хватило N нот».
- [x] `shared/lib/input/Input.ts` — D F J K / стрелки / pointer, таймстамп события переводится в аудиовремя.
- [x] `features/play-chart/model/GameSession.ts` — цикл: rAF только для рендера, логика от `Clock`.
- [x] Тесты судейства: 0.030 → perfect, 0.080 → great, 0.100 → good, 0.200 → miss (в GDD строка «0.100 → great» противоречила его же таблице окон — принята таблица).
- [x] v1.2: `Lives.ts` — 5 сердец, −1 за промах, +1 за 25 чистых попаданий, спелл «сердце»; ноль — провал без записи результата. Спеллы (`slow` — 6 с на 0.6× скорости, `heart`). Тач-помощь в `NoteManager` (`assistWindow` 0.4 с → Great). Секции с числом полос 2–6: `parseSections`, раскладки клавиш `KEY_LAYOUTS`, `Input` отдаёт код клавиши, сессия мапит его по активной секции.

## Готово когда
Тестовая карта проходится, счёт и комбо считаются верно, промах включает lowpass.
