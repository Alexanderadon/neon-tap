# Разбор 26.09: юзабилити, геймплей, дизайн

Источники: журнал нового игрока и четыре отчёта (UX, гейм-дизайн, визуал, техника). Всё проверено по коду, дубли слиты, споры решены. Порядок: польза × уверенность / цена.

## Сразу (S, по коммиту на пункт)

**1. Второй шанс без таймера.**
Почему: кольцо на 5 с у «Смотреть рекламу» давит на ребёнка ради ролика и тикает даже в свёрнутой вкладке. Заголовок «Сердца кончились» золотой, хотя золото значит «заработано».
Сделать: убрать действие `timeout`, кольцо и `REVIVE_OFFER_SEC`. Кадр ждёт выбора, задержка 1,2 с на кнопках остаётся. Заголовок белый, GDD поправить.
Нужно «да» владельца: отсчёт записан как его правило от 26.09.
Цена S. Файлы: features/play-chart/model/revive.ts, widgets/game-canvas/ui/GameCanvas.tsx (343–346, 636, 665), GDD.md.

**2. Первый забег: фора и мягкий провал.**
Почему: в Battle Theme первая нота на 0,52 с, и на 0:03 уже «ПРОВАЛ» маджентой и «Ранг D». С Bluetooth-наушниками без калибровки первая сессия на этом кончается.
Сделать: пока у игрока 0 ★ за всю игру, промахи в первые 15 с уровня 1 сердец не тратят. При провале вердикт «Ещё разок» белым, ранг не показывать, в FailFrame не рисовать три серые звезды. Тег «Дошёл до 0:03 из 1:56» оставить. Маджента — только промахи на поле (GDD:122).
Цена S. Файлы: features/play-chart/model/GameSession.ts (~697), widgets/result-breakdown/lib/verdict.ts, ui/ScorePanel.tsx, shared/i18n/parts/common.ts:55, GameCanvas.tsx (FailFrame).

**3. «Моя музыка»: выход всегда на месте.**
Почему: когда песня загружена, «Назад» меняется на «Ещё файл», свайпа и Escape нет. В колоду — только через Профиль, а «Обучение» в тройке запускает туториал заново.
Сделать: тройка «НАЗАД · ЕЩЁ ФАЙЛ · ПРОФИЛЬ» во всех состояниях, «Отменить» только на время анализа. `useSwipeBack` и Escape на CustomSongPage. Попутно: темп целым числом, поиск в списке показывать от 8 песен.
Цена S. Файлы: widgets/song-drop-zone/ui/SongDropZone.tsx (292–310), pages/custom/ui/CustomSongPage.tsx, widgets/my-music/ui/MyMusic.tsx.

**4. Колода: «Новинки», «Моя музыка» и трек дня в один тап.**
Почему: главная фича — 46-я карта, кнопка главы идёт «… → РОК-ПАК → ГЛАВА 1» мимо неё (`chapter.end % CATALOG.length`). Трек дня из колоды не найти. На ПК стрелки не работают без клика по колоде.
Сделать: круг кнопки главы «… → РОК-ПАК → НОВИНКИ → МОЯ МУЗЫКА → ГЛАВА 1». Пока трек дня сегодня не сыгран, в ряду главы справа стоит фишка-солнце (32 px, зона 44), тап листает к нему. ← → слушать на window, когда открыта колода и нет диалога. На карте «Новые треки» писать «С 5 октября» вместо второго «по понедельникам».
Цена S. Файлы: widgets/track-list/ui/TrackDeck.tsx (190–215), pages/menu/ui/MenuDock.tsx, shared/i18n/parts/drops.ts.

**5. Закрытые треки: путь через звёзды виден.**
Почему: серая «ОТКРЫТЬ ★ 0 из 13» не нажимается — «игра сломалась». У Hardstyler в магазине «★ ЕЩЁ 1 · 85 ◆», а лист покупки молчит, что трек откроется сам. В шторке наборов заранее выбран 700 ◆ за 99 ₽.
Сделать: кнопку на закрытой карте включить, серый вид оставить. По тапу — строка «Звёзды дают за уровни треков», и колода листается к ближайшему треку с недобранной звездой. В листе покупки при нехватке 3 ★ и меньше — строка «Или бесплатно: ещё N ★» и кнопка «Где взять звёзды». Тег карточки: «ЕЩЁ 1 ★ ДО ОТКРЫТИЯ». Набор по умолчанию — младший (300 ◆).
Цена S. Файлы: MenuDock.tsx (116–129), pages/menu/ui/MenuPage.tsx (onPlay), widgets/shop-grid/ui/PurchaseSheet.tsx, ShopCard.tsx, widgets/offer-popups/ui/CrystalsOffer.tsx:27.

**6. Результат и пауза: цифры сходятся, кнопки на своих местах.**
Почему: в бесконечном режиме HUD 278 700, пауза 288 600, результат 238 200 с «Новым рекордом»: ветка рекорда стоит раньше `endlessScore`, «всего N» не видно. «В МЕНЮ» после провала первая, после победы вторая. Дуэль предлагают и на провале. Монета «14 достиже…», пустая «+0 бонус». В паузе кошелёк 0 ◆ при ◆ 89 в HUD.
Сделать: при рекорде в бесконечном режиме — пара тегов «Новый рекорд | всего 288 600». Тройка: слот 1 всегда «В МЕНЮ», слот 2 «ЕЩЁ РАЗ» или «ПОДРОБНЕЕ», слот 3 «ДУЭЛЬ» только при 1 ★ и больше, иначе «РЕКОРДЫ». Иконку дуэли рисовать не из диагоналей креста. На монете подпись «достижения», число в углу; «+0» не показывать. В паузе фишка «+89 за забег».
Цена S. Файлы: ResultBreakdown.tsx (150–185, 258–278), result-breakdown/lib/loot.ts, shared/ui/icons.tsx:58, GameCanvas.tsx (пауза).

**7. Поле: одна подпись при смене полос, HUD не у края.**
Почему: смену полос рисуют дважды: цифра с «ПОЛОСЫ · ШИРЕ» (drawTransitionFx) и тег «4 полос · шире» (laneTag, без склонения), в обучении ещё карточка «Полосы: 4». На Android, ПК и в iframe Яндекса safeTop = 0: у «КРУГ 4» срезан верх. «+5» золотых сердец наезжает на звезду.
Сделать: в drawTransitionFx оставить только вспышку на 0,12 с. Подпись одна, laneTag со склонением через `plural`. В обучении не рисовать laneTag и чип точности. Верх HUD — `max(safeTop, 8)`. «+N» сдвинуть, чтобы до звезды было не меньше 8 px (сам «+N» утверждён 25.09).
Цена S. Проверить на 360 px без выреза. Файлы: features/play-chart/lib/Renderer.ts (drawTransitionFx, drawSongMap, drawHud), shared/i18n/parts/game.ts:5–6.

**8. Шапка: аватар по умолчанию и вход в профиль.**
Почему: на каждом экране слева «?» и обрезанное «NEON T…», и это не кнопка.
Сделать: без ника — аватар «кот» (картинка есть) без подписи; весь блок — кнопка в Профиль.
Цена S. Файлы: widgets/top-bar/ui/TopBar.tsx (51–59), shared/ui/Avatar.tsx.

## Доделать (M)

**1. Обучение: короче, по делу, с финалом.**
Почему: 16 шагов за ~65 с, финал на 6 полосах при правиле «не больше 5», слайды, дроби, круги и спиннер, которых в главе 1 нет. Шаги идут по времени — промахнувшись по всему, доходишь до 10/16. В конце сразу меню.
Сделать: 9 шагов, ~45 с, не больше 4 полос: intro, tap, hold, lanes2, alt, lanes3, lanes4, spell (сердце), finale. Если в tap, hold или lanes2 нет ни одного попадания, шаг один раз перематывается (NoteManager должен заново взвести ноты). Финал — кадр «Готово!» со строкой «В треке три уровня, каждый быстрее, за каждый звезда», «+20 ◆» и главной кнопкой «ИГРАТЬ · Battle Theme» прямо в забег. Остальные механики объясняет п. 5.
Цена M. Риск: tutorial.json (`npm run chart:tutorial`) и chart.test.ts править вместе. Файлы: features/tutorial/model/script.ts, scripts/gen-tutorial.mjs, public/charts/tutorial.json, pages/tutorial/ui/TutorialPage.tsx, GDD.md:88.

**2. Задержка подстраивается уже в обучении.**
Почему: калибровки при первом запуске нет, в обучении автоподстройка выключена (`autoOffset: !tutorial`), в забеге учится только на попаданиях в ±150 мс. Safari не сообщает outputLatency, Bluetooth добавляет 150–250 мс: промахи по всему, учиться не на чем. Отсюда провал на 0:03.
Сделать: на шагах с одной полосой брать каждое нажатие в ±300 мс от ближайшей ноты, одно на ноту. Если нажатий не меньше 8 и разброс (MAD) не больше 60 мс, сохранять медиану в `audioOffsetMs` до первого трека. `AUTO_TOTAL_MAX` поднять с 0,15 до 0,25. В забеге так же учиться на пустых нажатиях рядом с нотой.
Цена M. Файлы: GameSession.ts (124–129, 715, 743–760), GameCanvas.tsx:227, TutorialPage.tsx.

**3. Тишина в треках.**
Почему: в All Night Road (третья карта) первая нота на 28 с, в Up in the Sky после ноты на 0 с — 28 с пустоты, в Battle Theme пауза 12,5 с. Уровень играет песню с нуля: на трёх звёздах — полторы минуты ожидания, «игра зависла».
Сделать: если до первой «плотной» ноты больше 8 с, уровень начинается за 4 с до неё, одиночные ноты раньше сессия убирает. После паузы от 6 с над линией идёт «3 · 2 · 1» к следующей ноте.
Цена M. Риск: вступление слышно реже, согласовать с владельцем. Файлы: GameSession.ts (234, 387), songMap.ts, Renderer.ts.

**4. Рекорды и выбранный трек.**
Почему: «Рекорды» показывают только трек в фокусе. Карта «Моя музыка» не запоминается (readDeckIndex), и после магазина колода открывается на первом несыгранном треке: SS «пропал», кнопка сама стала «ИГРАТЬ Chill lofi».
Сделать: запоминать последний выбранный трек отдельно от номера карты, на картах без трека рекорды показывают его. В шапке рекордов стрелки ‹ › по сыгранным трекам.
Цена M. Файлы: widgets/track-list/model/deckPosition.ts, deckStart.ts, deckCards.ts:43, MenuPage.tsx (186–195), widgets/history-panel.

**5. Подсказка при первой встрече механики.**
Почему: глава 2 открывается Hardcore chiptune theme — 5 полос, слайды, дроби, спиннер, а в главе 1 были только тапы и холды. Обучение становится короче, объяснять надо здесь.
Сделать: перед первым слайдом, дробью, кругом и спиннером в обычном забеге показывать карточку TutorialOverlay за 2 доли до ноты, один раз (`settings.seenKinds`). По желанию первой картой главы поставить Sky Trance (1 слайд), Hardcore — дальше; уже открытые треки не закрывать.
Цена M. Файлы: widgets/tutorial-overlay, GameCanvas.tsx, entities/settings, entities/progress/model/unlocks.ts.

**6. Своя песня по силам игрока.**
Почему: воркер зовёт `composeChart` без `targetStars`, сложность — по энергии песни: ровный бит 120 BPM получил ★5 (95 нот за 40 с), с кругами и дробями, которых новичок не видел.
Сделать: передавать `targetStars = min(энергия, max(2, лучшая ★ пройденного трека + 1))`. В листе песни кнопка «Сложнее · ★N+1» пересобирает карту из сохранённого аудио.
Цена M. Файлы: features/generate-chart/model/analysis.worker.ts:25, widgets/my-music/ui/SongSheet.tsx.

**7. Телефон: лёгкий первый запуск и тишина в фоне.**
Почему: первый запуск тянет tutorial.mp3 на 4,6 МБ (150 с звука, ноты до 69 с). На iOS «Коснись экрана» с пульсирующими сегментами выглядит как загрузка. Шрифт с Google держит первый кадр. Радио меню играет в свёрнутом приложении (Яндекс запрещает). Экономный режим забывается после забега. Из загрузки песни не выйти.
Сделать:
- обрезать tutorial.mp3 до 75 с и сменить `MEDIA_CACHE`;
- при `tutorialDone=false` грузить за заставкой карту и песню обучения вместо обложек;
- в состоянии tap сегменты не пульсируют, текст «Коснись экрана, чтобы начать» (нового экрана не добавлять);
- Unbounded 400/700/900 в woff2 держать у себя, с preload;
- на `visibilitychange`/`pagehide` вызывать `stopAmbient()` и `ctx.suspend()`;
- запоминать `fxAuto='low'`;
- кнопка «В МЕНЮ» в кадре загрузки;
- радио: отменять загрузку при смене карты, ждать 900 мс, молчать при saveData.

Цена M (набор из S). Файлы: public/music/tutorial.mp3, public/sw.js, app/boot.ts, index.html, GameCanvas.tsx (213–216, 415–425), AudioEngine.ts, useDeckRadio.ts, songCache.ts.

**8. Тексты, цвета и иконки одним проходом.**
Почему: дети читают буквально, а цвет видят раньше цифры.
Сделать:
- «Ciptuned» → «Chiptuned», регистр названий в одном стиле;
- обложку Chill lofi с «CHLL LOFI» перерисовать и сменить `MEDIA_CACHE`;
- «Задержка» → «Подстройка», «Показывать FPS» только с `?debug`;
- «Пропустить» в калибровке возвращает в настройки;
- подпись «ЗВУК» в паузе меняется с состоянием;
- достижения в одном шаблоне;
- огонёк сложности без золота и мадженты: ★1–3 лайм, ★4–6 оранжевый;
- солнце только у трека дня, у анализа — песочные часы;
- канвас при prefers-reduced-motion без тряски.

Цена S–M. Файлы: assets-src/tracks.json, shared/i18n/parts/onboard.ts, pages/calibration/ui/CalibrationPage.tsx:20, shared/ui/difficultyColor.ts, shared/ui/icons.tsx, SongDropZone.tsx (302, 382), GDD.md:172.

## Потом (L или решение владельца)

**1. Юно и Яма в игре, а не только у цены.** Арт из yuno-yama-art.md по порядку: №6 на провал, №4 на финал обучения, №2 на карту «Моя музыка» вместо пустой ноты, №9 на пустые экраны. Герои только в окне покупки выглядят продавцами. Цена M на арт и S на место; генерирует владелец.

**2. Провал не отбирает пойманное и показывает рост.** Камни, долетевшие до счётчика, при провале пропадают (`saveResult.ts:54`). Зачислять их через запас дня, писать «на 12 с дальше прошлого раза» (`reachedSec` в истории). Это пересмотр правила «провал — 0 ◆» (economy-drops-mymusic.md §1.2). Цена M.

**3. Трек дня по силам.** Выбирается из всех 44 треков, 19 из них ★5–6: в 43 % дней новичку он не по силам. Пул — открытые и ближайшие закрытые с ★ не выше лучшей + 1; трек дня станет у всех разным. Цена M (daily.ts и тесты).

**4. Тренировка трудного места.** После провала вместо дуэли кнопка «ТРЕНИРОВКА»: 15 с от точки провала, скорость ×0,85, без сердец и без сохранения, 3 повтора. Цена L.

**5. Сборка для Яндекса и тяжёлая техника.** Облачное сохранение (`player.setData`, слияние по максимуму), `persist()` после первого трека, тишина на время рекламы; 20-секундные клипы для радио и превью, докачка через Range; плавные аудиочасы (за флагом, проверка на iPhone); один кадр на паузе вместо 60 в секунду. Цена L.

**6. Ждут слова владельца.** Треки для «Новинок» к 5 октября — все 13 слотов в drops.json пустые. Фон «rings» у orchestral и ambient похож на круг-ноту: заменить на haze (это поле). Экран NEON PASS — когда будет оплата.

## Что оставить как есть

- Схема экранов: шапка → контент → три кнопки → большая кнопка в цвет трека. Новые входы — в круг кнопки главы, не вкладками.
- Первый запуск сразу в обучение; в обучении нет сердец и провала.
- Прощающий ввод: пустое нажатие без штрафа, окна в реальных секундах, пропущенный бонус не отнимает сердце.
- Бюджет карт по ★, потолок ★6, одна карта на трек без выбора сложности.
- Экран победы: звёзды с чаймами, монеты в кошелёк, шоу звезды.
- Честный лист «цена · у тебя · останется», наборы только по «+», без автоокон.
- Время по аудиочасам, мультитач PointerLanes, пауза при сворачивании.

## Отклонено

- «Пауза 32×32, меньше 44» — зона нажатия уже 48×48 (game-canvas.css, `::before inset −9px`).
- «Обрезать тишину в конце уровня» — уже: `endTime` = последняя нота + 1,5 с (GameSession.ts:234).
- «Заполнить недели 1–4 треками achilles, shortcuts, technological-menace, hot-roadway» — владелец удалил эти треки 16.09 насовсем.
- «Золотые сердца вторым контуром вместо «+5»» — «+N» утвердил владелец 25.09 (GDD:143), правим только отступ.
- «Кадр с героями и кнопкой вместо «Коснись экрана»» — владелец запретил экраны-ворота для звука; меняем только вид состояния.
- «10 сердец при 0 ★» — выбрана фора 15 с: золотые сердца — награда за спелл, а фора бережёт именно первые секунды.
- «Сердца и звёзды в HUD серые» — серые только потерянные и незаработанные.
- «Ошибки консоли про REVIVE_PRICE, payForRevive…» — ссылок в коде нет, это следы HMR.
- «prefers-reduced-motion только в useSettled» — CSS учитывает его в десятке файлов; не учитывает канвас (Доделать, 8).
- «Сложность ★3–4 цианом» — циан по GDD значит действие и кристаллы.
- «GDD:386 запрещает таймеры» — строка про наборы; таймер убираем по правилу «без давления».
- «Трек дня золотым тегом в ряду главы» — золото значит «заработано»; вместо тега фишка-солнце.
