# Поток «Result polish» — экран результатов: где промахи, графики, повтор, шаринг

## План
- [x] Запись таймлайна в `GameSession` без аллокаций в кадре: `JudgementTimeline` — три типизированных массива
      (сантисекунды `Uint32Array`, код вердикта `Uint8Array`, комбо `Uint16Array`) размером `countJudgements`,
      конвертация один раз в `finish()` → `PlayResult.timeline` + `PlayResult.duration`.
- [x] Чистая аналитика `entities/score/lib/resultStats.ts`: лучшая серия, первый промах, худшее 10-секундное окно,
      бегущая точность, кривая комбо (ресемплинг ступенькой), выбор самого плотного 8-секундного отрезка. Тесты.
- [x] `widgets/result-breakdown`:
  - полоса песни (canvas): тики по вердиктам (miss — маджента, good — лайм, great — циан, perfect — белый),
    под ней полосы секций по числу полос, наведение/касание показывает m:ss;
  - график «точность + комбо» (inline SVG);
  - список моментов («Лучшая серия», «Первый промах», «Слабое место»);
  - «Повтор лучшего момента» — мини-поле на canvas, ноты падают по карте, вспышки на записанных временах,
    цикл ~8 с, пауза при скрытой вкладке и вне экрана; спрайты предрисованы (`shared/lib/render/neon`);
  - «Поделиться» — PNG-карточка 1080×1350 офскрин → `navigator.share({files})`, иначе скачивание;
    «Копировать» — текст в буфер обмена.
  - Слоты `extraTop` / `extraBottom` для других потоков (цель, история, лидерборд).
- [x] Блок словаря `// --- result ---` в `shared/i18n/ru.ts`.
